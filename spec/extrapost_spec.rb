require 'spec_helper'

describe Extrapost do
  it 'has a version number' do
    expect(Extrapost::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
